enum HttpStatusEnum
{
    OK = 200,
    CREATED = 201,
    INVALID = 400,
    UNAUTHORIZED = 401,
    NOT_FOUND = 404,
    METHOD_NOT_ALLOWED = 405,
    INTERNAL_SERVER_ERROR = 500,
}

export default HttpStatusEnum